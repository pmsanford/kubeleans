kind: Service
apiVersion: v1
metadata:
  name: web-port
  namespace: kubeleans
spec:
  type: NodePort
  selector:
    app: kubeleans-webapp
  ports:
  - name: http
    protocol: TCP
    port: 80
    nodePort: 30000